{
  "$schema": "https://json-schema.org/schema",
  "$id": "FhirServer",
  "title": "FHIR Server Generator",
  "description": "Generates a FHIR server project based on the HAPI FHIR JPA Starter",
  "type": "object",
  "properties": {
    "directory": {
      "type": "string",
      "description": "The directory to create the server in.",
      "x-prompt": "Enter the directory for the new server source code",
      "default": "server"
    },
    "packageBase": {
      "type": "string",
      "description": "The Java package path for custom code.",
      "x-prompt": "Enter the Java package path for your custom code",
      "default": "org.custom.server"
    },
    "release": {
      "type": "string",
      "description": "The HAPI FHIR JPA Starter release to use."
    },
    "fhirVersion": {
      "type": "string",
      "enum": ["STU3", "R4", "R4B", "R5"],
      "description": "The FHIR version to use for the server.",
      "default": "R4"
    }
  },
  "required": ["directory", "packageBase"]
}
