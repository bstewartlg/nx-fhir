{
  "$schema": "https://json-schema.org/schema",
  "$id": "Preset",
  "title": "",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What is the name of the workspace to create?",
      "default": "fhir-app"
    },
    "server": {
      "type": "boolean",
      "description": "Generate a FHIR server (nx-fhir:server) in the workspace",
      "x-prompt": "Generate a FHIR server project?",
      "default": true
    },
    "serverDirectory": {
      "type": "string",
      "description": "The directory to create the server in."
    },
    "packageBase": {
      "type": "string",
      "description": "The Java package path for custom code."
    },
    "release": {
      "type": "string",
      "description": "The HAPI FHIR JPA Starter release to use."
    },
    "fhirVersion": {
      "type": "string",
      "enum": ["STU3", "R4", "R4B", "R5"],
      "description": "The FHIR version to use for the server."
    }
  },
  "required": ["name", "server"]
}
